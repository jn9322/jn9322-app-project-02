import streamlit as st

st.write("# Welcome!")
st.write(
    "Purpose of this application is to parse data from XML -> validate them -> vizualize them -> and produce simple .txt sumary."
    )

st.write(
    "XML files/examples can be downloaded from this application as well."
    )

st.write(" ")

st.write("##### Process:") 
st.write("""
    1) Dowload XML (keep existing or you can update it) 
    2) Upload XML
    3) And see what happens... :)
    """
    )

st.write("enjoy...") 